我的hugo在cf上的pages部署，简单一点就是把打包的静态文件放上去就可以了
后面需要创建一个流水线然后绑定到我的git仓库中，cf会生成一个分支添加配置文件，可以再配置hugo的打包命令，配置文件中可以配置最终产物的位置，这样就可以了，实际上打包还会出现错误，是因为cf默认的hugo版本太低，然后我就在cf的构建页面添加了环境变量配置，把hugo的版本配置进环境变量里边。
还有个问题就是我push代码的时候 cf会默认进行打包构建，在这个，可是多半是因为缓存的原因吧，我最新的代码一直没有显示出来更新，